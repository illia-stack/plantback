RewriteEngine On

# 🔴 Force OPTIONS to be handled by PHP
RewriteCond %{REQUEST_METHOD} OPTIONS
RewriteRule ^(.*)$ $1 [QSA,L]

# Normal routing (optional but safe)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ $1 [QSA,L]